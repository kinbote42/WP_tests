This plugin uses a ReST API I wrote in Python to display random text returned from the API in either the header or the footer (based on a setting).
The ReST API uses OAuth2 Authentication (token is to be placed in the settings for the plugin)
